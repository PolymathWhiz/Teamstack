<% provide(:title, "Home")%>

<h1>Find me in Pages#Home</h1>

<%= render 'shared/devise_flash' %>

<% if user_signed_in? %>

<% if @sign_in_count == 1 %>
    <p> Welcome to Codezen </p>
<% end %>
  
    Logged in as <strong><%= truncate_user_name(current_user.first_name, current_user.last_name, 30) %></strong>.
      <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-link' %> |
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-link'  %>

<% end %>